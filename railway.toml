[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && python train_model.py --skip-if-exists 2>/dev/null; python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
